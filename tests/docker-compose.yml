version: '3'

services:
  service:
    image: nginx
    networks:
      - workspace
      - default
    labels:
      traefik.frontend.rule: Host:test.localhost
      traefik.enable: 'true'

networks:
  workspace:
    external:
      name: workspace_default
